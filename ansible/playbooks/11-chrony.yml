---
- name: Synchroniser l'horloge des hyperviseurs et des machines virtuelles
  hosts: proxmox:workloads
  gather_facts: false

  roles:
    - role: chrony
      when: pa_workload_kind | default('vm') != 'lxc'