---
- name: Générer les fichiers answer.toml pour l'installation Proxmox
  hosts: proxmox
  connection: local   # les nœuds n'existent pas encore : tout tourne sur le control node
  gather_facts: false

  roles:
    - pve_answer_file
